const store = {
  videos: new Map(),
  thumbnails: new Map(),
  captions: new Map(),
  workflowRuns: new Map(),
  tokens: new Map(),
  frames: new Map(),
  transcripts: new Map()
}

module.exports = { store }
